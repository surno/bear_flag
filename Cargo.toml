[package]
name = "bear_flag"
version = "0.1.0"
edition = "2021"

[dependencies]
image = { version = "0.24", default-features = false, features = ["png"] }
usvg = "0.45"
resvg = { version = "0.45", default-features = false, features = ["default"] }
